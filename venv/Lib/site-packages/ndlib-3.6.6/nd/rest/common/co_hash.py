# coding=utf-8
__author__ = 'linzh'

from itertools import izip


class CoHash(object):
    def __init__(self, map):
        self.dict = map

    def rev(self):
        d = self.dict
        return dict(izip(d.itervalues(), d.iterkeys()))